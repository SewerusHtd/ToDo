<div id='project_<%= @project.id %>' class='moving-project moving-project-<%= rem(@project.id, 4) + 1 %>'>
    <%= link(to: Routes.projects_path(@conn, :show, @project.id), class: :project, style: prepare_styles(@project)) do %>
        <h3><%= @project.title %></h3>
    <% end %>
</div>
